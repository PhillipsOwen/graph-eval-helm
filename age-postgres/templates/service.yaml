---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "age-postgres.fullname" . }}
  labels:
    {{- include "age-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "age-postgres.selectorLabels" . | nindent 4 }}
